# Cloudflare API令牌，可以从 https://dash.cloudflare.com/profile/api-tokens 获取
CF_API_TOKEN="your_api_token_here"

# 域名区域ID列表，可以从Cloudflare域名概述页面的右下角找到
# 支持配置多个域名，每行一个区域ID
CF_ZONE_IDS=(
    "your_zone_id_1_here"  # example.com 的区域ID
    "your_zone_id_2_here"  # test.example.com 的区域ID
) 